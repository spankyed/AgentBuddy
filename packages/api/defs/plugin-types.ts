/**
 * Plugin Types
 *
 * Re-exports all plugin event and entity types from default-setup.
 * Bundled by rollup into a standalone .d.ts so the API source doesn't
 * need a direct import path into default-setup.
 */

export type * from '@/__generated__/types';
