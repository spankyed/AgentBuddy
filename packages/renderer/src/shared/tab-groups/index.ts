export type { TabGroupColor, TabGroup } from './types';
export { ALL_COLORS } from './types';
export { getNextAvailableColor } from './colors';
export { saveTabGroups, loadTabGroups, clearTabGroups } from './persistence';
