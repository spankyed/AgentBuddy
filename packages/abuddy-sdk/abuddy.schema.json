{
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference for editor validation."
    },
    "$manifestVersion": {
      "type": "number",
      "const": 1,
      "description": "Schema version. Enables future format evolution."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Unique pack identifier. Lowercase, alphanumeric with hyphens."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable pack name."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+",
      "description": "Semver version string."
    },
    "builtIn": {
      "type": "boolean",
      "description": "Whether this pack is bundled with the host app."
    },
    "description": {
      "type": "string",
      "description": "Short description of the pack's purpose."
    },
    "hostVersion": {
      "type": "string",
      "description": "Semver range for compatible host versions (e.g. \">=0.3.0\")."
    },
    "license": {
      "type": "string",
      "description": "SPDX license identifier."
    },
    "dependencies": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Other packs this pack depends on. Keys are pack IDs, values are semver ranges or file/URL references."
    },
    "permissions": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "ears",
          "llm",
          "filesystem",
          "network",
          "terminal"
        ]
      },
      "description": "Capabilities this pack requires from the host."
    },
    "entities": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "not": {
          "enum": [
            "Relation",
            "Flow",
            "Node",
            "TNode",
            "Action",
            "Prompt"
          ]
        }
      },
      "description": "EARS entity types this pack registers, each key equal to its value, the type name ({ \"Memo\": \"Memo\" }). The SDK defines Relation, Flow, Node, TNode, Action, Prompt.",
      "propertyNames": {
        "not": {
          "enum": [
            "Relation",
            "Flow",
            "Node",
            "TNode",
            "Action",
            "Prompt"
          ]
        }
      }
    },
    "relKinds": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "not": {
          "enum": [
            "contains",
            "transitions_to",
            "instance_of",
            "spawned",
            "tracked"
          ]
        }
      },
      "description": "EARS relation kinds this pack registers. Keys are enum names, values are string identifiers. The SDK defines CONTAINS, TRANSITIONS_TO, INSTANCE_OF, SPAWNED, TRACKED.",
      "propertyNames": {
        "not": {
          "enum": [
            "CONTAINS",
            "TRANSITIONS_TO",
            "INSTANCE_OF",
            "SPAWNED",
            "TRACKED"
          ]
        }
      }
    },
    "partitionPolicy": {
      "type": "object",
      "properties": {
        "excludedEntityTypes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Entity types excluded from persistence (in-memory only)."
        }
      },
      "additionalProperties": false,
      "description": "Built-in packs only. Ignored for external packs."
    },
    "entityShapes": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "Source file path relative to pack root."
          },
          "type": {
            "type": "string",
            "description": "Exported TypeScript type name for the entity attributes."
          }
        },
        "required": [
          "source",
          "type"
        ],
        "additionalProperties": false
      },
      "description": "Maps entity type strings to their TypeScript attribute interfaces for type-safe EARS queries."
    },
    "features": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-zA-Z0-9]*$",
            "description": "Unique feature identifier. A lowercase-first identifier (letters and digits), used as a name in generated code; not a reserved word or \"busId\"."
          },
          "designation": {
            "type": "string",
            "description": "Links the system to an EARS designation."
          },
          "settings": {
            "type": "string",
            "description": "Path to default settings file."
          },
          "typesEntry": {
            "type": "string",
            "description": "Additional types to include in the generated type barrel."
          },
          "earlySystem": {
            "type": "boolean",
            "description": "Start this feature's system before EARS hydration. Built-in packs only."
          },
          "system": {
            "type": "object",
            "properties": {
              "entry": {
                "type": "string",
                "description": "Path to the backend system module."
              },
              "outgoingEventsType": {
                "type": "string",
                "description": "TypeScript type name for outgoing events (used by codegen)."
              },
              "sendsTo": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Plugins this system sends events to besides its own feature's, named as code names them: other features of this pack that have a plugin (by feature id), a dependency's plugins (\"<packId>/<featureId>\"), or host plugins (\"application\"). Another feature of this pack gains this system's outgoing events; a dependency's plugin or a host plugin keeps the events its own owner declares it receives, and naming it here is what makes it sendable at all."
              },
              "events": {
                "type": "object",
                "properties": {
                  "incoming": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "description": "Event types this system listens for."
                  }
                },
                "additionalProperties": false,
                "description": "Event routing declarations."
              }
            },
            "required": [
              "entry"
            ],
            "additionalProperties": false,
            "description": "Backend system module."
          },
          "plugin": {
            "type": "object",
            "properties": {
              "entry": {
                "type": "string",
                "description": "Path to the frontend plugin module, which default-exports the Plugin (its id, label, icon and isPinned)."
              },
              "default": {
                "type": "boolean",
                "description": "Show this plugin when the app starts. At most one of a pack's features may claim it; the first pack to register one across the app wins."
              }
            },
            "required": [
              "entry"
            ],
            "additionalProperties": false,
            "description": "Frontend plugin definition."
          },
          "services": {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "pattern": "^[^#]+#[A-Za-z_$][\\w$]*$"
            },
            "propertyNames": {
              "pattern": "^[A-Za-z_$][\\w$]*$"
            },
            "description": "Services. Keys are service names on `services`, values are \"path#exportName\" of the service object (an object literal or a class instance, not a factory) in a source file."
          },
          "repositories": {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "pattern": "^[^#]+#[A-Za-z_$][\\w$]*$"
            },
            "propertyNames": {
              "pattern": "^[A-Za-z_$][\\w$]*$"
            },
            "description": "Repository objects. Keys are repository names on `repository` (from #generated/repository), values are \"path#exportName\" of the object in a source file."
          },
          "references": {
            "type": "string",
            "description": "Path to a module declaring which of this feature's things are linkable from an editor (protocol, category, icon, navigate). Built-in packs only. Ignored for external packs."
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": false
      },
      "description": "Feature definitions. Each feature bundles a backend system, frontend plugin, services, and settings."
    },
    "packServices": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "pattern": "^[^#]+#[A-Za-z_$][\\w$]*$"
      },
      "propertyNames": {
        "pattern": "^[A-Za-z_$][\\w$]*$"
      },
      "description": "Pack-level services not tied to a specific feature. Keys are service names on `services`, values are \"path#exportName\" of the service object (an object literal or a class instance, not a factory) in a source file."
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$",
            "description": "The command as typed after the \"/\", without it."
          },
          "placeholder": {
            "type": "string",
            "minLength": 1,
            "description": "What the chat shows after the command: the argument it takes, or what it does."
          }
        },
        "required": [
          "name",
          "placeholder"
        ],
        "additionalProperties": false
      },
      "description": "Slash commands this pack adds to the chat. Sending one fires a `user.command` event the pack's flows handle; a name must be unique across the app."
    },
    "boot": {
      "type": "object",
      "properties": {
        "hooks": {
          "type": "string",
          "description": "Module exporting lifecycle hooks: onInit (after EARS hydration, before migrations and seeds) and onShutdown (when the pack's backend stops)."
        },
        "seed": {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "properties": {
                  "path": {
                    "type": "string",
                    "description": "Source directory or file, relative to the pack root."
                  },
                  "format": {
                    "type": "string",
                    "pattern": "^(?:([a-z][a-z0-9-]*):)?([a-z][a-z0-9-]*)$",
                    "description": "The format compiling `path`: a name in this pack's seedFormats, or \"<dependency id>:<name>\" for a dependency's."
                  },
                  "seeder": {
                    "type": "string",
                    "description": "A pack module exporting seed(ctx), used instead of the generic seeder. Alone, the build compiles nothing for the entry and the module brings its own data; with \"path\" and \"format\", the module seeds the compiled records."
                  }
                },
                "additionalProperties": false
              }
            ]
          },
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "description": "Seed data sources. Keys are seed names; the specialty keys (actions, prompts, flows) take a path, other keys an entry object."
        },
        "seedPolicy": {
          "type": "object",
          "properties": {
            "skipAtBoot": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Seed types to skip during boot."
            },
            "skipAfterOnboarding": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Seed types to skip after onboarding completes."
            }
          },
          "additionalProperties": false,
          "description": "Controls which seed types to skip at boot or after onboarding."
        }
      },
      "additionalProperties": false,
      "description": "Boot sequence configuration."
    },
    "steps": {
      "type": "object",
      "properties": {
        "register": {
          "type": "string",
          "description": "Path to the step registration barrel file."
        },
        "build": {
          "type": "string",
          "description": "Path to a module exporting build-time step definitions only (validate/compile/decompile, trigger facets; no runtime or FE imports). Bundled to build/steps.build.mjs for dependent packs."
        },
        "definitions": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "description": "Step type identifier."
              },
              "path": {
                "type": "string",
                "description": "Directory containing the step definition."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "step",
                  "trigger"
                ],
                "description": "Whether this is a regular step or a trigger."
              },
              "dsl": {
                "type": "object",
                "properties": {
                  "primaryField": {
                    "type": "string",
                    "description": "The field whose value becomes the step label in the flow editor."
                  },
                  "defaultLabel": {
                    "type": "string",
                    "description": "Fallback label when primaryField is empty."
                  },
                  "custom": {
                    "type": "boolean",
                    "const": true,
                    "description": "Marks the step as a custom (non-built-in) type."
                  }
                },
                "additionalProperties": false,
                "description": "DSL configuration for flow-helper generation."
              }
            },
            "required": [
              "type",
              "path"
            ],
            "additionalProperties": false
          },
          "description": "Step definitions for codegen."
        }
      },
      "required": [
        "register",
        "definitions"
      ],
      "additionalProperties": false,
      "description": "Flow step definitions."
    },
    "artifacts": {
      "type": "string",
      "description": "Path to artifact type registration module."
    },
    "blocks": {
      "type": "string",
      "description": "Path to message block registration module."
    },
    "migrations": {
      "type": "string",
      "description": "Path to migrations index module."
    },
    "fe": {
      "type": "object",
      "properties": {
        "tiptapPlugins": {
          "type": "string",
          "description": "Path to tiptap plugin registration module."
        },
        "appExtensions": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "propertyNames": {
            "pattern": "^[A-Za-z_$][\\w$]*$"
          },
          "description": "Named app extensions. Keys are extension names (identifiers), values are paths to Vue components."
        },
        "bundleUi": {
          "type": "boolean",
          "description": "Bundle a copy of @abuddy/ui into the pack instead of using the host app's. All of @abuddy/ui is bundled, so the pack never mixes the two."
        }
      },
      "additionalProperties": false,
      "description": "Frontend-specific pack configuration."
    },
    "dsl": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "entry": {
            "type": "string",
            "description": "Path to the DSL type definition module."
          },
          "targets": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "monaco"
              ]
            },
            "description": "Editor targets for intellisense integration."
          },
          "prefix": {
            "type": "string",
            "description": "Namespace prefix for DSL symbols."
          },
          "globals": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "description": "Global type mappings injected into the DSL scope."
          },
          "inline": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Packages whose declarations are bundled into the editor definitions, besides @abuddy/* and the pack's own modules. The editor loads no node_modules, so a type it needs from another package belongs here."
          }
        },
        "required": [
          "entry",
          "targets"
        ],
        "additionalProperties": false
      },
      "description": "DSL type definitions for Monaco editor intellisense."
    },
    "seedFormats": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "format": {
            "type": "string",
            "enum": [
              "markdown-tree",
              "json"
            ],
            "description": "A built-in format: a directory of markdown, or a JSON array of records."
          },
          "compiler": {
            "type": "string",
            "description": "A module in this pack whose default export compiles an entry's path into records. Used instead of \"format\"."
          },
          "entity": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "minItems": 1
              }
            ],
            "description": "The entity types the format's records seed. Omitted, entries using it are compiled but not seeded."
          },
          "identity": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "Fields matched to find an existing row (\"parent\" = the tree parent). Ignored for entity types whose owning pack registers a find seed hook."
          },
          "tree": {
            "type": "object",
            "properties": {
              "branch": {
                "type": "string",
                "description": "A directory's own markdown file (e.g. \"index.md\"), giving the directory's frontmatter and body."
              },
              "branchEntity": {
                "type": "string",
                "description": "The entity type directories seed. Defaults to `entity`."
              },
              "relKind": {
                "type": "string",
                "description": "The relation from a parent row to each child row. Defaults to \"contains\"."
              }
            },
            "additionalProperties": false,
            "description": "Walk subdirectories as parent rows."
          },
          "fields": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "properties": {
                "from": {
                  "type": "string",
                  "pattern": "^(body|filename|path|frontmatter\\.[\\w-]+)$",
                  "description": "Where the value comes from: the markdown body, the display name of the file or directory, its relative path, or a frontmatter field."
                },
                "default": {
                  "description": "Used when the source is absent. The string \"filename\" means the display name."
                },
                "type": {
                  "type": "string",
                  "const": "string",
                  "description": "Coerce a present value to a string (YAML reads an unquoted 2024 as a number)."
                }
              },
              "required": [
                "from"
              ],
              "additionalProperties": false
            },
            "description": "Record fields for markdown-tree: field name → where its value comes from."
          },
          "media": {
            "type": "string",
            "pattern": "^(?!\\.{1,2}(?:\\/|$))(?!.*\\/\\.{1,2}(?:\\/|$))[^/\\\\:]+(?:\\/[^/\\\\:]+)*$",
            "description": "A directory under an entry's path copied with the seeds; media/<file> links become media://<id>/<file>."
          }
        },
        "additionalProperties": false
      },
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "description": "Named seed formats: how a source becomes records. boot.seed entries name one; dependents name them as \"<pack id>:<name>\"."
    },
    "seedHooks": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "pattern": "^[^#]+#[A-Za-z_$][\\w$]*$"
      },
      "description": "Seed hooks for entity types this pack declares: entity type → \"path#exportName\" of a SeedHooks object. Any pack seeding the type uses them."
    }
  },
  "required": [
    "id",
    "name",
    "version"
  ],
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentbuddy.dev/schemas/abuddy.json",
  "$comment": "Auto-generated from manifest-schema.ts — do not edit by hand. Run: npm run generate:schema",
  "title": "AgentBuddy Pack Manifest",
  "description": "Configuration manifest for an AgentBuddy pack. Declares metadata, features, entities, services, extensions, seeds, and build-time codegen inputs."
}
