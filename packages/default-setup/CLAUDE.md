# Default Setup

Seeds, systems, and plugins for the default AgentBuddy experience.

- System code: `src/features/<name>/be/system.ts`
- Plugin code: `src/features/<name>/fe/`
- Plugin registry: `src/registries/plugins.ts`
- Settings: `src/seeds/default-settings.ts` (pack-wide) + per-plugin `settings.ts`
