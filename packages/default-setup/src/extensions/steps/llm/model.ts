import type { ModelId } from '@abuddy/sdk/models';

/** The model an llm node runs when it names none, and a new node starts with: an id from the model catalog */
export const DEFAULT_MODEL: ModelId = 'anthropic:claude-opus-5';
