// The host `settings` feature's frontend, as the renderer pairs it with the Settings view's components.
export { createSettingsMachine, type SettingsContext, type SettingsEvents, type SettingsIO, type SettingsSave, type SettingsState, type SettingsTarget } from './machine.ts';
