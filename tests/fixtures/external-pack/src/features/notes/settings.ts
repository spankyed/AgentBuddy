// Keyed by this feature's id, as any pack's is: the app stores it under this pack's own plugin id,
// so it never touches default-setup's `notes` settings.
export default {
  plugins: {
    notes: { listTitle: 'Fixture Notes' }
  }
}
