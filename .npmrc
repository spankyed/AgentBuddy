# Node processes npm starts in this checkout (scripts, npx playwright, tsx) resolve workspace
# @abuddy/* packages to their source; see the @abuddy/source condition in their package.json exports
node-options=--conditions=@abuddy/source
