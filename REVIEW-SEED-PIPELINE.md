Act as a senior DevOps engineer to review and rearchitect the build/seed pipeline between `packages/default-setup` and `packages/api`. Currently the flow is fragile: source `.ts` files are compiled to JSON in `default-setup/dist/`, then `sync-seed` copies them to `api/src/setup/seed/data/`, then the API build copies them again to `api/dist/setup/seed/data/` via a `cp -r` that breaks on repeat builds. At runtime the seed loader reads from `api/dist/` using `__dirname`, which serves stale data if any step is missed or misordered. The `seeded` flag means data only loads once unless force-reseeded. Review the full pipeline (`compile → sync:seed → build:be → seed at startup`), identify redundant copies, fragile path resolution, and ordering dependencies, then propose a cleaner architecture that eliminates the intermediate `api/src/setup/seed/data/` copy and makes the compiled output reliably available at runtime with minimal manual steps. Reference `packages/default-setup/CLAUDE.md` and `packages/api/src/setup/seed/index.ts` for context.
