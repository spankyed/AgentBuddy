```
# Goal: compile @abuddy/ui, share it from the host, then switch to `.ts` specifiers

Implement docs/issues/goal-compiled-ui-host-shared.md on branch AS/pack-type-facades:
Background, Decisions, Phases, Constraints. Read it first. Decisions are final:
implement them, don't reopen them or stop to ask. Where a detail isn't specified,
pick the conventional option, note it in the final summary, and keep going.

Finished when:
- Phase 0's spike results are recorded in this doc and Phases 1–4 meet their "Done when".
- Every new guard or test is mutation-checked.
- `npm run typecheck`, `npm run typecheck -w @app/main`, `schema:check`, `api:check`
  (sdk, ui), `packages:build` + `packages:check`, and the api, sdk, host, cli,
  default-setup and renderer unit suites pass.
- `npm run test:external-pack`, the smoke E2E, `npm run test:packaged-authoring` and
  the example pack's `abuddy test --app-root <repo>` (8 tests) pass.
- A final summary: phase → done, evidence, and the conventional choices made.

Never:
- push or tag. Commit as you go in logical chunks (conventional messages, no
  Co-Authored-By or session lines); check `git diff --cached` and stage only each
  commit's files.
- npm publish, create GitHub releases, or trigger workflows (dry runs only).
- pkill/killall Electron or node; launch the app outside the test env without an
  isolated ABUDDY_USER_DATA_DIR.
- run bare tsc on packages/preload, `npm install` in the example pack, or edit
  version/release metadata.
- loosen a failing assertion instead of investigating.
```

## Background

- `@abuddy/ui` ships its `.vue` files as source (`dist/**/*.vue`), and each pack's Vite build compiles them and inlines the components into the pack's `fe.js` (`packages/abuddy-cli/src/build/fe-bundler.ts`). Only `@abuddy/sdk/fe` and the libraries in `SDK_FE_MODULES` / `SHARED_DEPS` (`packages/abuddy-host/src/build/shared-deps.ts`) come from the host through `window.__abuddy`, which the renderer's `virtual:host-deps` plugin builds (`packages/renderer/vite.config.ts`).
- Consequences of inlining:
  - Every pack that uses `TiptapEditor` or the Monaco editors ships its own copy of them and of lowlight.
  - Stateful UI modules (e.g. `components/monaco-config.ts`) get a separate instance per pack. The DSL type registry already had to move into `@abuddy/sdk/fe` for this reason.
  - Packs depend on their Vite and `@vitejs/plugin-vue` setup matching how our SFCs are written.
- The `.ts` specifier spike (`docs/issues/goal-ts-import-specifiers.md`) failed only because of the shipped SFC source: `tsc` rewrote the SDK's and host's JS correctly, but copied `.vue` files kept `./x.ts` imports that don't exist in `dist/`.
- What other libraries do (published tarballs and build configs checked 2026-09-13):
  - 19 of 20 Vue component libraries publish compiled JS as the runtime: Reka UI (tsdown, `dts: { vue: true }`), Ark UI (Vite `preserveModules` + vite-plugin-dts), Element Plus (Rollup + plugin-vue, `preserveModules`), PrimeVue (Rollup), Vuetify, Naive UI, Ant Design Vue, Quasar, Headless UI, Vue Flow, Tiptap Vue 3. Nuxt UI ships `.vue` source, and only works with its own Nuxt/Vite plugin in the consumer's build.
  - Plugin hosts provide the UI kit at runtime. Grafana's plugin build marks `@grafana/ui`, `@grafana/runtime` and `@grafana/data` external by default (`bundleGrafanaUI: false` opts into bundling); `@grafana/ui` is compiled and uses a source export condition like ours. Directus shares `vue`, `vue-router`, `vue-i18n`, `pinia` and its SDK with extensions and registers its app components globally.
  - Reka UI's tsdown config avoids per-file ("unbundle") output for SFCs ("creates a lot of unwanted files because of the multiple sections of SFC files") and relies on shared chunks.

## Decisions

Final.

1. **`@abuddy/ui` publishes compiled JS.**
   - A Vue-aware bundler compiles SFCs and `.ts` modules into ESM. Phase 0 picks tsdown or Vite library mode with `preserveModules`. `vue`, `@abuddy/sdk` and every host-shared library are external.
   - Modules shared by several entries (e.g. `monaco-config`, the tiptap extensions) are emitted once, as a shared module or chunk, never duplicated per entry.
   - Declarations stay per module from vue-tsc (`X.d.vue.ts`). The explicit exports map stays generated by `scripts/exports.ts`, with every export keeping `types`, `@abuddy/source` and `default`.
   - Component exports drop `.vue`: `@abuddy/ui/design/button`, `@abuddy/ui/components/tiptap/TiptapEditor`. Their `@abuddy/source` target stays the `.vue` file, so the monorepo still needs no build step.
   - Component CSS (scoped styles, `tiptap-theme.css`) is emitted as CSS the compiled module imports, so a consumer's bundler collects it.
   - This amends `goal-ts-import-specifiers.md` Decision 1 for `@abuddy/ui` only: a Vue compiler decides its output. No script post-processes that output or rewrites specifiers. `@abuddy/sdk` and `@abuddy/host` stay `tsc`-only.
2. **The host provides `@abuddy/ui` to packs by default (the Grafana model).**
   - The renderer exposes every `@abuddy/ui` export on `window.__abuddy` (one namespace keyed by export subpath), next to the existing SDK modules.
   - The pack FE bundler turns every `@abuddy/ui/*` import into a proxy to the host's module, the way it handles `@abuddy/sdk/fe`. Component exports proxy their default export.
   - `abuddy.json` `fe.bundleUi: true` opts a pack into inlining all of `@abuddy/ui` instead. It's all or nothing, so a pack never mixes host and bundled instances of the same modules. The pack's Tailwind config then also scans `node_modules/@abuddy/ui/dist`.
   - Compatibility is the same contract as `@abuddy/sdk/fe`: packs declare `hostVersion`, and `@abuddy/ui`'s public API changes follow semver, checked by its API reports.
   - The host-registry guard keeps working for inlined modules (`fe.bundleUi`).
3. **`.ts` relative specifiers in `@abuddy/sdk`, `@abuddy/host` and `@abuddy/ui`, after Decisions 1–2 land.**
   - sdk and host: `allowImportingTsExtensions` + `rewriteRelativeImportExtensions`, emitted by `tsc` (validated by the spike).
   - ui: the bundler resolves `.ts` imports, and vue-tsc typechecks and emits declarations with `allowImportingTsExtensions` only. With the rewrite flag, vue-tsc reports TS2876 on `.vue` imports.
   - Consumers that compile this source get the flags the spike recorded: api `allowImportingTsExtensions` + `rewriteRelativeImportExtensions`; cli, testing, host, ui and the `facade-typing` test `allowImportingTsExtensions`; `scripts/bundle-package.ts` `--allowImportingTsExtensions`.
   - Declarations keep `.ts` specifiers (TypeScript doesn't rewrite them). The spike showed consumers resolve them under node16 and bundler, and the published consumer tests keep checking that.
   - Generated pack code (`generate-entries.ts`) keeps `.js`.
   - Not `erasableSyntaxOnly`.
4. **A guard rejects relative `.js` specifiers** in the three packages' `.ts` and `.vue` sources. Use oxlint's `import/extensions` rule if it can express "`.ts` required, `.js` rejected" for relative imports; otherwise a small test. The nodenext typecheck against extensionless imports stays.

## Phases

### Phase 0 — Spikes (throwaway branch or worktree)

Record the results in a "Spike results" section of this doc before Phase 1.

**A. Build tool.** Build `@abuddy/ui` with tsdown and with Vite library mode + `preserveModules`, both with `vue`, `@abuddy/sdk` and host-shared libraries external. For each, record:
1. Whether shared modules are emitted once. Grep `dist/` for the definition of a module-level variable in `components/monaco-config.ts` and check that it appears in exactly one file.
2. Output layout vs the 81 exports, and whether vue-tsc's declarations line up with each JS entry (attw, `published-ui-types`, `published-exports`).
3. CSS: scoped styles and `tiptap-theme.css` end up in CSS the compiled modules import.
4. Build time and `dist/` size.

Pick the tool that passes 1–3. If both pass, pick the one with fewer config lines and no custom output plugins. If neither passes 1, stop and record why in this doc.

**B. Host sharing.** On top of A's output:
1. The renderer exposes every UI export on `window.__abuddy`. Record the renderer's main chunk size before and after (`npm run build -w @app/renderer`); the difference must be under 5%, since the renderer already imports most components statically.
2. A pack importing `@abuddy/ui/components/tiptap/TiptapEditor` and `@abuddy/ui/composables/useDebounce` builds with no UI code in its `fe.js` (grep), renders the editor in E2E, and shares the host's `monaco-config` state.

### Spike results (2026-09-13)

Tools: Vite 7.0.6 with `@vitejs/plugin-vue`; tsdown 0.21.10 (rolldown 1.0.0-rc.17) with unplugin-vue 7.2.0 and `@tsdown/css` 0.21.10. Both builds took entries from the exports map and kept dependencies and peers external.

**A. Build tool: tsdown.**

| Check | Vite library mode + `preserveModules` | tsdown (bundled entries, shared chunks) |
|---|---|---|
| 1. Shared modules once | ✅ `registeredDslLibs = new Set()` only in `components/monaco-config.js` | ✅ only in `components/monaco-config.js` (per-file `unbundle` mode too) |
| 2. Layout, declarations | Two files per component (`X.js` re-exporting `X.vue.js`). Not taken further. | One `X.js` per export (82 JS incl. 2 shared chunks). With vue-tsc declarations in the real package: attw exit 0, `publint --strict` "All good!", `published-ui-types` 2/2, `published-exports` 2/2. |
| 3. CSS | ❌ One `ui.css` no module imports; with `cssCodeSplit: true`, 17 component CSS files and still no imports. Needs a third-party CSS-injection plugin. | ✅ With `css: { inject: true }` each component imports its CSS (15 imports, 14 files). Two findings: unplugin-vue under tsdown can't resolve `<style src="./tiptap-theme.css">` (ENOENT; it resolves from the working directory), so `TiptapEditor.vue` uses `<style>@import "./tiptap-theme.css";</style>`; and the `@import` is only inlined with the `postcss` transformer (tsdown loads `postcss-import` for it). The default left `@import "./tiptap-theme.css"` in the emitted CSS, which a pack build then failed on, so `transformer: 'postcss'` is explicit and a dist test rejects unresolved relative `@import`s. |
| 4. Time, size | 2 s, 788 KB (127 JS, 1 CSS) | ~1 s compile; the package's `dist/` is 916 KB with declarations (82 JS, 14 CSS, 80 declarations) |

tsdown passes 1–3; Vite fails 3.

**B. Host sharing: passes.**
1. The renderer main chunk went from 4,389,492 to 4,423,287 bytes (+0.77%, limit 5%) when `virtual:host-deps` imported all 80 UI exports. JS chunks went from 42 to 39: `BaseForm`, `BaseNode` and `TipSection`, previously lazy step-form chunks, moved into the main chunk. Baseline: the last build of the same renderer code before the change.
2. A pack importing `TiptapEditor` and `useDebounce` builds a `fe.js` holding only `window.__abuddy["@abuddy/ui/…"]` proxies (no `createExtensions`, `ProseMirror` or debounce code), in the workspace and the published layout. The fixture pack renders the host's `TiptapEditor` in E2E (`memos.spec.ts`), and a test loads a built pack against the host's `monaco-config` module and gets the same function objects.

### Implementation notes

- Decision 1's "their `@abuddy/source` target stays the `.vue` file" doesn't work for types: TypeScript ignores exports targets with a `.vue` extension, and vue-tsc only maps specifiers that end in `.vue`. It went unnoticed while a built `dist/` existed, because TypeScript fell back to its declarations; from a clean checkout the renderer and default-setup couldn't resolve `@abuddy/ui/design/ToastNotification`. Tsconfig `paths` to `.vue` files didn't help either. Each public component therefore has a `.ts` entry module (`design/button.ts` re-exporting `./button.vue`, like PrimeVue's per-component `index` modules), and the exports map lists `.ts` modules only. Component exports stay extensionless, and the monorepo still needs no build step.

### Phase 1 — Compile `@abuddy/ui`

- Build with the tool from Phase 0 (`packages/abuddy-ui/scripts/build-package.ts`). Keep the undeclared-import guard and `assertExportTargetsBuilt`.
- `scripts/exports.ts` generates extensionless component exports. Migrate imports in default-setup, renderer, the fixture and example packs, `tests/scripts/test-packaged-authoring.sh`, the `published-ui-types` and `published-exports` tests, and docs.
- Update the renderer's and default-setup's tsconfig/shims if extensionless `.vue` exports need them for types in the monorepo (`@abuddy/source` → `.vue`).

**Done when:**
- `dist/` contains no `.vue` files.
- A pack that inlines `@abuddy/ui` (today's behavior until Phase 2) builds without `@vitejs/plugin-vue` compiling any `@abuddy/ui` file (a test fails if the pack's Vite build transforms a `.vue` file under `node_modules/@abuddy/ui`).
- attw, publint, `published-ui-types` (wrong prop still fails) and `published-exports` pass.
- A shared module's state variable is defined in one emitted file (test).

### Phase 2 — Host-shared `@abuddy/ui`

- Renderer: expose every UI export on `window.__abuddy`, generated from `@abuddy/ui`'s exports map (not a hand list).
- `@abuddy/host/build/shared-deps`: add the UI modules to the host-shared module list. `fe-bundler.ts`: proxy `@abuddy/ui/*` unless `fe.bundleUi`; discover proxy exports from the resolved module (compiled JS, or `.vue`/`.ts` source under `@abuddy/source`).
- `abuddy.json`: add `fe.bundleUi` to `manifest-schema.ts`, regenerate `abuddy.schema.json`, document it.
- Update the SDK bridge drift spec and `fe-bundler-host-registry.spec.ts` for the new host-shared modules.

**Done when:**
- A pack importing `TiptapEditor` and `useDebounce` has no `@abuddy/ui` code in `fe.js` (test; mutation: removing the proxy fails it).
- The same pack with `fe.bundleUi: true` inlines them and passes the host-registry guard.
- A test shows a pack and the host share one instance of a stateful UI module.
- The renderer main chunk grew less than 5% (recorded).
- `test:packaged-authoring` passes with its pack importing `TiptapEditor` and `SimpleMonacoEditor`, and a backend-only pack still installs no tiptap or highlight.js.

### Phase 3 — `.ts` specifiers

- Codemod relative `.js` specifiers to `.ts` in `packages/abuddy-sdk`, `packages/abuddy-host` and `packages/abuddy-ui` (src, tests, `.vue` `<script>` blocks). Imports of hand-written declarations without a `.ts` source (`fe/electron-api.d.ts` → `./speech-event.js`) stay `.js` and are exempt from the guard.
- Apply Decision 3's flags. Rebuild `packages/default-setup/dist/dev-entry.cjs` before the api suites.
- Add the Decision 4 guard to `npm run typecheck` or CI.

**Done when:**
- `grep` finds no relative `.js` specifier in the three packages' sources except the exemption.
- Emitted JS in `packages/abuddy-sdk/dist`, `packages/abuddy-ui/dist` and the `@abuddy/cli` and `@abuddy/testing` bundles (which inline `@abuddy/host`) has no relative `.ts` specifier (test).
- The guard fails on an added `./x.js` import (mutation).

### Phase 4 — Docs and CI

- Update `CLAUDE.md` (SDK packages section: compiled UI, host-shared by default, `fe.bundleUi`, specifier convention), `docs/public-facing/architecture.md` (shared modules, `@abuddy/ui`), `getting-started.md`, `packages/abuddy-testing/CLAUDE.md` if affected, the build-package comments, and the "Spike result" note in `goal-ts-import-specifiers.md` pointing here.
- CI (`.github/workflows/ci.yml`) runs the new guard and tests.

**Done when:** docs describe no `.vue` imports from `@abuddy/ui`, no `.js` relative specifier convention, and CI runs the guard.

## Constraints

- Commit as you go in logical chunks, with conventional messages and no Co-Authored-By or Claude-Session lines. Check `git diff --cached` before each commit and stage only that commit's files. Never push or tag.
- Never publish externally: no `npm publish` (use `npm pack` and `--dry-run`), no real GitHub releases. CI workflows may be written, not triggered.
- Never use broad pkill/killall on Electron or node. E2E runs alongside the user's dev and prod apps in the `abuddy-test` namespace.
- Don't launch the app outside the test environment without isolating `ABUDDY_USER_DATA_DIR`.
- Never run bare tsc on `packages/preload`. Don't run `npm install` in the example pack (edit its source; link new workspace packages into its `node_modules` like its existing links). Don't edit version/release metadata.
- Investigate failing tests before changing assertions; mutation-check every new guard or test.
- Prefer libraries over hand-rolled code. No polling or hacky workarounds.
- External packs are first-class. Keep the in-repo fixture pack, the example pack (`/Users/spankyed/Develop/Projects/abuddy-external/example-pack`) and `test:packaged-authoring` passing throughout.
- Manual API boots: `cd packages/api && ABUDDY_ENV=development ABUDDY_USER_DATA_DIR=<copy> NODE_ENV=development API_PORT=3099 BUILT_IN_PACKS_DIR=$PWD/.. node --conditions=@abuddy/source dist/server.js`.
